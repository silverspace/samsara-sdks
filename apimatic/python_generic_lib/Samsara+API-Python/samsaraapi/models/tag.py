# -*- coding: utf-8 -*-

"""
    samsaraapi

    This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
"""

import samsaraapi.models.tagged_address
import samsaraapi.models.tagged_asset
import samsaraapi.models.tagged_driver
import samsaraapi.models.tagged_machine
import samsaraapi.models.tagged_sensor
import samsaraapi.models.tagged_vehicle

class Tag(object):

    """Implementation of the 'Tag' model.

    TODO: type model description here.

    Attributes:
        addresses (list of TaggedAddress): The addresses that belong to this
            tag.
        assets (list of TaggedAsset): The assets that belong to this tag.
        drivers (list of TaggedDriver): The drivers that belong to this tag.
        group_id (long|int): The GroupID that this tag belongs to.
        id (long|int): The ID of this tag.
        machines (list of TaggedMachine): The machines that belong to this
            tag.
        name (string): Name of this tag.
        parent_tag_id (long|int): If this tag is part a hierarchical tag tree
            as a child tag, the parentTagId is the ID of this tag's parent
            tag.
        sensors (list of TaggedSensor): The sensors that belong to this tag.
        vehicles (list of TaggedVehicle): The vehicles that belong to this
            tag.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id":'id',
        "name":'name',
        "addresses":'addresses',
        "assets":'assets',
        "drivers":'drivers',
        "group_id":'groupId',
        "machines":'machines',
        "parent_tag_id":'parentTagId',
        "sensors":'sensors',
        "vehicles":'vehicles'
    }

    def __init__(self,
                 id=None,
                 name=None,
                 addresses=None,
                 assets=None,
                 drivers=None,
                 group_id=None,
                 machines=None,
                 parent_tag_id=None,
                 sensors=None,
                 vehicles=None):
        """Constructor for the Tag class"""

        # Initialize members of the class
        self.addresses = addresses
        self.assets = assets
        self.drivers = drivers
        self.group_id = group_id
        self.id = id
        self.machines = machines
        self.name = name
        self.parent_tag_id = parent_tag_id
        self.sensors = sensors
        self.vehicles = vehicles


    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object as
            obtained from the deserialization of the server's response. The keys
            MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary
        id = dictionary.get('id')
        name = dictionary.get('name')
        addresses = None
        if dictionary.get('addresses') != None:
            addresses = list()
            for structure in dictionary.get('addresses'):
                addresses.append(samsaraapi.models.tagged_address.TaggedAddress.from_dictionary(structure))
        assets = None
        if dictionary.get('assets') != None:
            assets = list()
            for structure in dictionary.get('assets'):
                assets.append(samsaraapi.models.tagged_asset.TaggedAsset.from_dictionary(structure))
        drivers = None
        if dictionary.get('drivers') != None:
            drivers = list()
            for structure in dictionary.get('drivers'):
                drivers.append(samsaraapi.models.tagged_driver.TaggedDriver.from_dictionary(structure))
        group_id = dictionary.get('groupId')
        machines = None
        if dictionary.get('machines') != None:
            machines = list()
            for structure in dictionary.get('machines'):
                machines.append(samsaraapi.models.tagged_machine.TaggedMachine.from_dictionary(structure))
        parent_tag_id = dictionary.get('parentTagId')
        sensors = None
        if dictionary.get('sensors') != None:
            sensors = list()
            for structure in dictionary.get('sensors'):
                sensors.append(samsaraapi.models.tagged_sensor.TaggedSensor.from_dictionary(structure))
        vehicles = None
        if dictionary.get('vehicles') != None:
            vehicles = list()
            for structure in dictionary.get('vehicles'):
                vehicles.append(samsaraapi.models.tagged_vehicle.TaggedVehicle.from_dictionary(structure))

        # Return an object of this model
        return cls(id,
                   name,
                   addresses,
                   assets,
                   drivers,
                   group_id,
                   machines,
                   parent_tag_id,
                   sensors,
                   vehicles)


