# -*- coding: utf-8 -*-

"""
    samsaraapi

    This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
"""


class Day(object):

    """Implementation of the 'Day' model.

    TODO: type model description here.

    Attributes:
        active_hours (float): Hours spent on duty or driving, rounded to two
            decimal places.
        active_ms (long|int): Milliseconds spent on duty or driving.
        certified (bool): Whether this HOS day chart was certified by the
            driver.
        certified_at_ms (long|int): Unix epoch time (in ms) of time when this
            chart was certified. If this chart is uncertified, 0.
        distance_miles (float): Distance driven in miles, rounded to two
            decimal places.
        end_ms (long|int): End of the HOS day, specified in milliseconds UNIX
            time.
        start_ms (long|int): End of the HOS day, specified in milliseconds
            UNIX time.
        trailer_ids (object): List of trailer ID's associated with the driver
            for the day.
        vehicle_ids (object): List of vehicle ID's associated with the driver
            for the day.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "active_hours":'activeHours',
        "active_ms":'activeMs',
        "certified":'certified',
        "certified_at_ms":'certifiedAtMs',
        "distance_miles":'distanceMiles',
        "end_ms":'endMs',
        "start_ms":'startMs',
        "trailer_ids":'trailerIds',
        "vehicle_ids":'vehicleIds'
    }

    def __init__(self,
                 active_hours=None,
                 active_ms=None,
                 certified=None,
                 certified_at_ms=None,
                 distance_miles=None,
                 end_ms=None,
                 start_ms=None,
                 trailer_ids=None,
                 vehicle_ids=None):
        """Constructor for the Day class"""

        # Initialize members of the class
        self.active_hours = active_hours
        self.active_ms = active_ms
        self.certified = certified
        self.certified_at_ms = certified_at_ms
        self.distance_miles = distance_miles
        self.end_ms = end_ms
        self.start_ms = start_ms
        self.trailer_ids = trailer_ids
        self.vehicle_ids = vehicle_ids


    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object as
            obtained from the deserialization of the server's response. The keys
            MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary
        active_hours = dictionary.get('activeHours')
        active_ms = dictionary.get('activeMs')
        certified = dictionary.get('certified')
        certified_at_ms = dictionary.get('certifiedAtMs')
        distance_miles = dictionary.get('distanceMiles')
        end_ms = dictionary.get('endMs')
        start_ms = dictionary.get('startMs')
        trailer_ids = dictionary.get('trailerIds')
        vehicle_ids = dictionary.get('vehicleIds')

        # Return an object of this model
        return cls(active_hours,
                   active_ms,
                   certified,
                   certified_at_ms,
                   distance_miles,
                   end_ms,
                   start_ms,
                   trailer_ids,
                   vehicle_ids)


